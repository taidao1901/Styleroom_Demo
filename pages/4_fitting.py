import streamlit as st
from utilities.fitting import components
st.set_page_config(layout="wide",initial_sidebar_state='collapsed')
def main():
    left, mid, right  = st.columns([1,1,1])
    with mid:
        re
        components.show_image()